双线性差值 
实现操作：
1.计算新图像和原始图像比例因子
2.利用比例因子计算新图像目标像素位置对应的原始图像中的像素位置
3.由虚拟像素位置找到周围的四个点的像素点
4.利用双线性差值方法来计算出像素值

直方图均衡化
利用opencv中的eauqalizeHist函数


sobel边缘检测
利用opencv中的sobel函数，cv2.sobel
x = cv2.Sobel(img,cv2.CV_16S, 1,0 )
y = cv2.Sobel(img,cv2.CV_16S, 0,1 )
