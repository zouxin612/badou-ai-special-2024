图像放大注意点：1.定义函数 2.定义原图的参数，尺寸和通道 3.定义一个新的空图像 4.计算缩放比例 5.遍历每个元素  
6.用最邻近插值法，使得新图像的新增的像素点就近取值   7取值结果赋值  8.打印图像组合，imshow&waitKey(0)

灰度化注意点：1.读取图像  2.定义一个和原图一样的新图像  3.遍历每一个像素点，用浮点算法将原图变为单通道图像（也可以用cv2.cvtCOLOR(图像，cv2.COLOR_BGR2GRAY))
二值化注意点：1.二值化要在灰度化图像的基础上进行  2.确定临界值，超过这个值输出为1，不超过这个值输出为0.5 3.输出的时候plt.imshow(img_binary, cmap='gray')后面的cmap不要忘了 
